# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_MVP_LOGIN_ENABLED=false

# Google OAuth (redirect happens via backend, but frontend needs client ID for sign-in button)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Feature flags
NEXT_PUBLIC_AI_ENABLED=true
